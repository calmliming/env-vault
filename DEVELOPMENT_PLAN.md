# EnvVault 开发计划

## 1. 项目目标

EnvVault 是一个本地优先的桌面工具，用于管理分散在不同目录中的项目配置文件和大模型调用凭据。

核心目标：

- 扫描任意目录下的 `.env`、`.env.local`、`.env.development`、`.env.test`、`.env.production` 等文件。
- 完整提取 `.env*` 文件中的所有配置项，不因为变量名称或厂商归属而丢弃记录。
- 将大模型厂商凭据从通用配置中区分出来，只管理调用地址和 API Key。
- 通过项目、环境和变量名绑定，把中心记录同步回原项目文件。
- 默认隐藏敏感值，减少复制、展示和提交到 Git 的风险。
- 所有数据默认保存在本机，不上传明文 Key。

## 2. 产品边界

### 2.1 通用配置库

通用配置库记录 `.env*` 中的每一项：

```text
项目 / 环境 / 来源文件 / 变量名 / 变量值 / 值类型 / 敏感等级 / 同步状态
```

示例变量包括：

```text
DATABASE_URL
NEXT_PUBLIC_API_URL
JWT_EXPIRES_IN
ENABLE_CACHE
PORT
OPENAI_API_KEY
```

即使变量属于模型调用，也先作为 `.env` 来源记录保留，再由用户选择是否关联到模型凭据。

### 2.2 模型凭据库

模型凭据是独立实体，只保存：

```text
厂商名称
调用地址（Base URL）
API Key
```

可以额外保存非敏感元数据：Key 指纹、末四位、状态、创建时间、标签和备注。

首版不保存账号余额、模型列表、组织成员或厂商业务数据。Azure、代理平台等需要额外字段时，采用可扩展字段 schema，但默认界面仍只展示地址和 Key。

### 2.3 明确不做

- 首版不做账号系统、多人协作和云端明文同步。
- 首版不替代厂商控制台，不负责创建或撤销厂商 Key。
- 首版不强制统一所有项目的变量命名。
- 首版不自动覆盖用户未确认的文件修改。

## 3. 技术架构

### 3.1 推荐技术栈

```text
桌面：Electron
渲染层：React + TypeScript
样式：原生 CSS 设计令牌，后续可迁移到组件库
本地索引：SQLite + better-sqlite3
系统密钥库：Windows Credential Manager / macOS Keychain / Linux Secret Service
文件监听：chokidar 或 Electron 主进程文件监听封装
解析：dotenv parser，保留注释、空行、顺序和引号
```

### 3.2 进程边界

```text
主进程
├── 文件读取、写入、监听
├── SQLite 访问
├── 加密与系统密钥库
├── Git 状态检查
└── IPC 命令校验

Preload
└── 只暴露白名单 API

渲染进程
└── 页面状态、表格、表单和交互
```

Electron 安全基线：

- `nodeIntegration: false`
- `contextIsolation: true`
- `sandbox: true`
- 渲染进程不直接访问 Node.js 或文件系统。
- 不加载远程页面和远程脚本。
- IPC 参数进行类型、路径和权限校验。
- 设置严格 CSP，发布包启用代码签名。

## 4. 数据模型

### 4.1 项目与文件

```text
projects
- id
- name
- absolute_path
- git_root
- tags
- created_at
- last_opened_at

env_files
- id
- project_id
- environment
- absolute_path
- file_hash
- parser_version
- last_scanned_at
```

### 4.2 通用配置项

```text
config_entries
- id
- env_file_id
- key
- encrypted_value
- value_type
- sensitivity
- source_line
- original_format
- credential_binding_id nullable
- updated_at
```

`original_format` 用于恢复引号、空格、注释关联和变量顺序，避免写回时破坏文件可读性。

### 4.3 模型凭据与绑定

```text
model_credentials
- id
- provider_name
- endpoint
- encrypted_api_key
- fingerprint
- last_four
- status
- tags
- notes
- last_validated_at

credential_bindings
- id
- credential_id
- project_id
- environment
- endpoint_variable
- key_variable
- last_synced_hash
- sync_mode
```

关系规则：一个模型凭据可以绑定多个项目和环境；一个项目环境中的地址变量名和 Key 变量名可以不同。

## 5. 信息架构与页面

### 5.1 主导航

```text
配置总览
模型凭据
安全检查
操作记录
设置
```

### 5.2 配置总览

- 左侧列出所有已连接项目，不要求位于同一个父目录。
- 项目内切换 development、test、staging、production。
- 中间展示当前环境下所有配置项。
- 支持按变量名、来源文件、类型和同步状态筛选。
- 支持查看差异、编辑、复制、删除和同步到文件。
- `.env*` 中的普通配置和模型相关配置都在此处可见。

### 5.3 模型凭据

列表字段：

```text
厂商 | 凭据名称 | 调用地址 | Key 尾号 | 状态 | 绑定项目数 | 最近验证
```

详情操作：

- 新增或编辑调用地址。
- 新增或轮换 API Key。
- 默认掩码，临时显示完整值。
- 一键复制并在设定时间后清理剪贴板。
- 查看绑定项目和环境。
- 验证凭据，不显示完整请求头。
- 批量同步到选定项目。

### 5.4 安全检查

- `.env.local` 是否被 `.gitignore` 覆盖。
- 敏感文件是否已被 Git 跟踪。
- 是否存在疑似 Key、Token、私钥或高风险变量。
- 中心记录与本地文件是否存在差异。
- 项目配置文件最近修改时间和来源。

### 5.5 操作记录

记录显示、复制、导入、修改、同步、验证和轮换等动作；只保存元数据，不保存完整敏感值。

## 6. 核心工作流

### 6.1 添加项目与扫描

1. 用户选择任意项目目录。
2. 应用定位 Git 根目录并扫描 `.env*`、`.gitignore`。
3. 展示发现的文件和变量数量，不立即修改文件。
4. 用户确认纳入管理的文件。
5. 解析全部变量并加密导入。
6. 建立文件哈希和监听任务。
7. 执行 Git 风险检查。

### 6.2 识别与关联模型凭据

1. 根据变量名和 URL 给出厂商建议。
2. 同时保留原始通用配置记录。
3. 用户确认后创建模型凭据。
4. 用户指定地址变量名和 Key 变量名。
5. 建立项目/环境绑定。
6. 后续凭据轮换时，通过绑定关系定位受影响文件。

### 6.3 编辑与同步

1. 在内存中解密目标值。
2. 展示修改前后差异。
3. 用户确认同步范围。
4. 写入临时文件并校验内容。
5. 使用原子替换更新目标文件。
6. 更新文件哈希、同步状态和操作记录。

### 6.4 外部修改

```text
文件监听
→ 发现哈希变化
→ 重新解析
→ 显示中心记录与文件差异
→ 用户选择导入文件或恢复中心记录
```

任何外部修改在用户确认前都不能被覆盖。

### 6.5 凭据轮换

1. 用户在厂商控制台创建新 Key。
2. 在模型凭据页面添加新版本。
3. 应用验证地址和 Key。
4. 展示所有受影响的项目和环境。
5. 用户选择同步范围并确认差异。
6. 原子更新目标 `.env*` 文件。
7. 用户在厂商控制台撤销旧 Key。
8. 旧版本标记为 revoked，保留不含明文的审计记录。

## 7. 安全设计

- 主密钥只保存在操作系统密钥库。
- SQLite 中的敏感值只保存密文。
- 解密值只在必要的内存生命周期内存在。
- 应用锁定时清理内存中的解密缓存。
- 完整 Key 默认不展示，显示操作需二次确认。
- 搜索结果、日志、错误、通知和崩溃报告禁止出现完整 Key。
- 复制操作支持自动清理剪贴板，默认 30 秒。
- 写文件前创建备份或可恢复临时文件。
- 导出配置必须二次确认，并优先导出加密包。
- 验证请求使用厂商元数据接口，避免无意产生推理费用。

## 8. 厂商适配策略

首批适配：

```text
OpenAI / Anthropic / Google Gemini / DeepSeek / OpenRouter
```

第二批适配：

```text
Azure OpenAI / 阿里云百炼 / 火山引擎 Ark / 智谱 / Moonshot / MiniMax / Mistral / Groq
```

每个适配器提供：

```text
providerName
credentialSchema
defaultEndpoint
detectFromVariableName
validate(endpoint, key)
redact(value)
```

支持自定义厂商：用户填写厂商名称、调用地址、Key 变量映射和验证方式（关闭验证也可保存）。

## 9. 分阶段开发计划

### 阶段 0：基础设施与产品骨架

交付：

- Electron + React + TypeScript 工程。
- 主进程、Preload、渲染进程目录和 IPC 约定。
- 本地数据库迁移机制。
- 系统密钥库读写封装。
- 当前 HTML 原型转为组件和路由。

验收：应用可以启动、创建本地数据库、锁定/解锁 Vault，并通过最小 IPC 读写健康状态。

### 阶段 1：项目与 `.env*` 全量扫描

交付：

- 任意目录选择和项目注册。
- `.env*` 文件发现与解析。
- 全量变量入库，包括普通变量和模型变量。
- 注释、顺序、空行、引号格式保留。
- 文件哈希和外部修改监听。

验收：选择一个包含多个 `.env*` 文件的项目后，所有键和值都能在界面中逐项查看，重新写回不改变无关格式。

### 阶段 2：通用配置工作台

交付：

- 项目、环境、文件和变量层级导航。
- 搜索、筛选、编辑、删除、复制和掩码显示。
- 差异预览、原子写入、失败恢复。
- 文件修改后的导入/恢复决策。

验收：用户可以只修改一个变量并同步到指定文件，其他配置、注释和顺序保持不变。

### 阶段 3：模型凭据库

交付：

- 厂商、调用地址和 Key 的独立存储。
- 从 `.env*` 变量建议识别模型凭据。
- 一个凭据绑定多个项目和环境。
- 地址变量名、Key 变量名自定义映射。
- 首批 5 个厂商验证适配器。

验收：同一个凭据修改一次，可以预览并同步到多个项目；通用配置页面仍能看到原始来源和绑定状态。

### 阶段 4：安全中心与轮换

交付：

- Git tracked 检查和 `.gitignore` 检查。
- 疑似敏感值检测和风险分级。
- 凭据版本、轮换、停用和影响范围预览。
- 操作记录和剪贴板清理。

验收：被 Git 跟踪的敏感文件必须显示风险；轮换旧 Key 时能准确列出受影响的项目和环境。

### 阶段 5：CLI 与高级集成

交付：

- `envvault run -- <command>` 进程注入。
- 从模板生成 `.env.example`。
- 加密导出，以及**导入加密导出包**（这一条原文写作「批量导入」，
  下面 §12 有它的澄清；「扫一个目录、勾一批文件入库」那个意义上的批量导入
  在阶段 1 就交付了）。
- 1Password、Bitwarden、Doppler 等外部 Secret Manager 适配。

验收：CLI 注入模式可以在不落盘明文 Key 的情况下启动本地开发命令。

## 10. 测试计划

### 单元测试

- `.env` 解析和格式保留。
- 引号、空值、多行值、变量引用和注释。
- 值类型和敏感等级判断。
- Key 掩码、指纹和重复检测。
- 加密/解密和密钥库失败处理。
- 路径校验和 IPC 参数校验。

### 集成测试

- 多目录项目扫描。
- 多环境配置导入。
- 文件外部修改与差异处理。
- 凭据绑定与批量同步。
- 原子写入失败恢复。
- Git tracked 和 `.gitignore` 规则。

### 端到端测试

- 新增项目 → 扫描 → 导入 → 编辑 → 同步。
- 导入模型 Key → 绑定两个项目 → 轮换 → 批量同步。
- 锁定 Vault 后敏感值不可见。
- 复制 Key 后剪贴板按时清理。

### 发布前检查

- Windows 首发包安装、升级、卸载。
- Electron 安全配置审计。
- 依赖漏洞扫描。
- 代码签名和自动更新完整性。
- 不同分辨率和窄屏布局检查。

## 11. MVP 验收清单

全部完成（阶段 0~4）。每一条后面括号里是守着它的验收断言所在的阶段。

- [x] 可以添加任意位置的多个项目。（阶段 1）
- [x] 可以发现所有 `.env*` 文件。（阶段 1）
- [x] `.env*` 中的所有配置项都被记录。（阶段 1，含重复 key）
- [x] 配置值加密保存，默认掩码。（阶段 1；阶段 2 补上了 `original_format` 的漏）
- [x] 可以按项目和环境查看、搜索、编辑配置。（阶段 2）
- [x] 可以预览差异并安全写回文件。（阶段 2，原子写入 + 备份 + 并发校验）
- [x] 模型凭据独立保存厂商、调用地址和 Key。（阶段 3）
- [x] 一个模型凭据可以绑定多个项目和环境。（阶段 3）
- [x] 可以从 `.env*` 变量创建凭据绑定。（阶段 3）
- [x] 可以检查 `.gitignore` 和 Git 跟踪状态。（阶段 4a）
- [x] 可以记录外部修改、复制、显示和同步操作。（阶段 1 起；复制在 4b 独立成一类）
- [x] Electron 渲染进程不能直接访问文件系统。（阶段 0，`verify:ui` 逐条守着）

## 12. 当前进度与下一步

阶段 0~4 已完成，阶段 5 的 CLI 注入（5a）与 `.env.example` 生成（5b）也已完成。
落地记录分别见 `docs/PHASE-0.md`（含与本计划的两处偏差及其理由）、`docs/PHASE-1.md`、
`docs/PHASE-2.md`、`docs/PHASE-3.md`、`docs/PHASE-4A.md`、
`docs/PHASE-4B.md`、`docs/PHASE-5A.md`、`docs/PHASE-5B.md`。
新会话请先读 `docs/HANDOFF.md`：那里写着「现在到哪了、有哪些坑、下一步做什么」。

阶段 4 拆成了两刀，因为它的验收句本来就是两半：
4a「被 Git 跟踪的敏感文件必须显示风险」、
4b「轮换旧 Key 时能准确列出受影响的项目和环境」，两半都已完成。

阶段 5 同样拆开了：5a（CLI 注入，本节的验收句）、5b（`.env.example` 生成）、
5c（加密导出与导入导出包）。5b 单独一刀是因为模板是**唯一一个设计上要进 Git 的
产物**，边界和前面几条「值离开本进程」都不一样；5c 的导出则是又一条明文出口。

**§11 的 MVP 验收清单到这里全部成立。** 剩下的是阶段 5。

原始 HTML 原型保留在 `index.html` 作为视觉对照，运行中的界面在 `src/renderer/`。

下一步开发顺序：

1. ~~将原型拆分为 Electron 主进程、Preload 和 React 渲染层。~~（阶段 0 已完成）
2. ~~先完成 `.env*` 解析与全量配置入库。~~（阶段 1 已完成）
3. ~~补上文件监听与 §6.4 的决策流程，做变量级差异与原子写回。~~（阶段 2 已完成）
4. ~~补上编辑与删除单个变量的入口。~~（阶段 2 已完成）
5. ~~接入模型凭据实体、项目绑定与厂商验证请求。~~（阶段 3 已完成）
6. ~~阶段 4a：Git 跟踪与 `.gitignore` 检查、风险分级、安全检查页。~~（已完成）
7. ~~阶段 4b：凭据版本与停用、轮换前的影响范围预览、剪贴板定时清理。~~（已完成）
8. ~~阶段 5a：`envvault run` 进程注入。~~（已完成）
9. ~~阶段 5b：`.env.example` 生成。~~（已完成）
10. 阶段 5c：加密导出与导入导出包。加密导出用**用户口令 + KDF**，
    不是系统密钥库派生 —— 密钥库派生的包只有本机能解，而换机器/备份正是它的用途。

    > **§9 里「批量导入」指的就是「导入加密导出包」**，是导出的镜像操作。
    > 这个词从阶段 0 起被一路抄进各版文档却从没定义过，直到 5b 收尾时才澄清。
    > 它**不是**「一次纳管多个项目」—— 那件事现在确实做不到
    > （`scanProject` 只认一个项目根，把 workspace 根目录丢进去会把所有仓库的
    > `.env` 拍平成同一个项目，Git 风险检查还会定位到错的仓库根），
    > 但那是扫描层和数据模型的活，和导出无关，要做就单独一刀。
    > 也**不是**「从别的工具的格式导入」—— 那和第 11 条的外部 Secret Manager
    > 适配在意图上重叠。
11. 外部 Secret Manager 适配（1Password / Bitwarden / Doppler）——
    需要机器上装有各自的 CLI，动手前先想清楚怎么验收。
